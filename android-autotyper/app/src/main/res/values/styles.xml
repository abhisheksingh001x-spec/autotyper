<?xml version="1.0" encoding="utf-8"?>
<!--
    styles.xml — YE FILE MISSING THI, ISLIYE "Theme.AutoTyper" error aa rha tha.
    AppTheme AppCompatActivity ko extend karta hai jo sab appcompat features deta hai.
-->
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/windowBackground</item>
    </style>

    <!-- No action bar variant (optional, for full-screen activity) -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
